"use client";

/**
 * Route distance/tolls are computed on Search press in Booking
 * (avoids races that cleared results on mobile).
 */
export default function OneStopTollCalculator() {
  return null;
}
